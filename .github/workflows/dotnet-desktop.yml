name: .NET Core Full Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  # # Build Job
  # build_backend:
  #   runs-on: ubuntu-latest

  #   env:
  #     Solution_Name: Backend.sln

  #   steps:
  #   - name: Checkout
  #     uses: actions/checkout@v4

  #   - name: Install .NET Core
  #     uses: actions/setup-dotnet@v4
  #     with:
  #       dotnet-version: 8.0.x

  #   - name: Restore Dependencies
  #     run:  dotnet restore 

  #   - name: Build Solution
  #     run: dotnet build --configuration Release
      
  #   - name: Execute unit tests
  #     run: dotnet test UnitTests/UnitTests.csproj

  #   - name: Build and push Docker image
  #     uses: docker/build-push-action@v4
  #     with:
  #       context: .
  #       file: dockerfile.api
  #       push: true
  #       tags: bjand49/tonl-api
  #     env:
  #       DOCKER_USERNAME: bjand49
  #       DOCKER_PASSWORD: ${{ secrets.DOCKER_PAT }}


  build_seeder:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install .NET Core
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Cache NuGet Packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('DataSeeder/**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-

    - name: Build Docker Image
      run: |
        docker build . \
          -f dockerfile.seeder \
          -t bjand49/dataseeder:latest

    - name: Login to Docker Hub
      uses: docker/login-action@v1
      with:
        username: bjand49
        password: ${{ secrets.DOCKER_PAT }}

    - name: Push Docker Image
      run: docker push bjand49/dataseeder:latest
